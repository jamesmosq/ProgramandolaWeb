Compila los assets de frontend del proyecto EduCode y verifica el resultado.

Pasos:
1. Corre `npm run build`
2. Confirma que se generó `public/build/manifest.json`
3. Muestra el tamaño del CSS y JS compilados
4. Si hay error de Vite o Tailwind, muestra el output completo
